# libp2p-mulitlang-gossip

- sample programs to send messags from js to rust libp2p node.
- browser support for gossipsub is limited so browser and wasm implementation are broken and still in development 
